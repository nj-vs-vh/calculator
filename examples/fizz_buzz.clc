func fizz_buzz(n) {
    custom_line = {
        if mod(n, 3) == 0 "Fizz" else ""
    } + {
        if mod(n, 5) == 0 "Buzz" else ""
    };
    print(if length(custom_line) > 0 custom_line else n);
};
i = 1;
while i < 100 {
    fizz_buzz(i);
    i = i + 1;
};